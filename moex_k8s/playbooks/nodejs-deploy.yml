# playbooks/nodejs-deploy.yml
---
- name: Deploy Node.js on k8s host
  hosts: k8s
  become: yes
  gather_facts: yes
  vars_files:
    - ../group_vars/dev/vault.yml

  roles:
    - docker_setup
    - nodejs
