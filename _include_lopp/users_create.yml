- name: Создание пользователей nginx
  user:
    name: nginx
    shell: /bin/bash
    groups: nginx
    append: yes
#    home: "/app/{{ item.apdir }}"
#  with_items:
#      - { usname: nginx, apdir: nginx }
#      - { usname: httpserver, apdir: httpserver }

- name: Создание пользователей httpserver
  user:
    name: httpserver
    shell: /bin/bash
    groups: httpserver
    append: yes
